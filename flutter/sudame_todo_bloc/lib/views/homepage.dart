import 'package:flutter/material.dart';

class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('sudame ToDo BLoC'),
      ),
      body: Center(
        child: Text('sudame ToDo BLoC'),
      ),
    );
  }
}
